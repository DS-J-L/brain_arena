# 공개 전 개인정보 점검

점검일: 2026-09-07 / 기준 HEAD: `2c1e3fe`

## 보완 및 이력 재작성 결과

2026-09-07 로컬 이력 재작성을 완료했습니다. 아래 최초 점검 결과와 커밋 목록은 **재작성 전 상태**입니다.

- 기존 커밋 12개의 Author / Committer를 `DS-J-L <DS-J-L@users.noreply.github.com>`으로 교체했습니다. GitHub 계정 귀속 여부는 별도로 확인하지 않았습니다.
- 로컬 `main` 및 `origin/main` 추적 ref를 재작성된 이력으로 변경했습니다. 원격 서버 자체는 변경하지 않았습니다.
- 새 HEAD는 `379da29bbd1839a19e0d78100846c5ef0d819a8a`입니다. 12개 커밋 각각의 파일 트리와 커밋 메시지, 작성 시각을 보존했습니다.
- 도달 가능한 전체 커밋을 다시 검사하여 기존 작성자 식별자가 헤더에서 제거됐음을 확인했습니다.
- 저장소 단위 `user.name`, `user.email`도 공개용 정보로 변경했습니다.
- `.gitignore`에 `.env.*`, `!.env.example`, `.privacy-backup/`을 추가했습니다.
- 원본 이력 백업은 `.privacy-backup/before-privacy-rewrite.bundle`, 이전/새 커밋 대응표는 `.privacy-backup/commit-map.json`입니다. 백업은 검증했으며 Git 추적에서 제외했습니다.
- 백업과 로컬 reflog/도달 불가능 Git 객체에는 원래 개인정보가 남아 있을 수 있습니다. 일반 브랜치 push 대상은 아니지만 작업 폴더나 `.git` 전체를 공유하면 안 됩니다.
- 2026-09-08 원격 반영 준비 단계에서 원격 HEAD가 기존 `2c1e3fecd8c2f51d0e97c10edb8724330b043afe`와 동일함을 확인했습니다. 이 값을 명시한 `--force-with-lease`로 원격의 새 변경을 덮어쓰지 않도록 보호합니다. 푸시 후 원격 HEAD와 로컬 HEAD의 일치 여부를 별도 검증합니다.
- `.gitignore` 및 이 보고서는 공개용 작성자 정보로 작성한 보완 커밋에 포함합니다.

## 결론

개인정보 비공개가 목적이라면 **커밋 작성자 정보를 정리한 뒤 공개**해야 합니다. 로컬에서 확인한 12개 커밋 모두 Author와 Committer에 개인 Naver 이메일(`d***@naver.com`)과 동일한 작성자 이름(`D***`)이 들어 있습니다. 이름이 실명인지는 판단하지 않았습니다.

파일 내용에서는 검사한 패턴에 해당하는 실제 개인정보나 인증 비밀값을 발견하지 못했습니다. 이는 개인정보가 전혀 없다는 보증은 아닙니다.

## 발견 항목과 조치

| 우선순위 | 항목 | 위치 및 범위 | 조치 |
| --- | --- | --- | --- |
| 높음 | 개인 이메일 및 작성자 이름 | 아래 12개 커밋의 Author / Committer | 이메일 공개를 원하지 않으면 과거 이력을 재작성하여 GitHub noreply 이메일과 공개용 이름으로 교체 |
| 높음 | 향후 커밋에도 같은 정보 사용 | 현재 유효한 Git `user.name`, `user.email`이 과거 작성자 정보와 일치 | 다음 커밋 전에 저장소 단위 작성자 설정 변경 |
| 참고 | 배포 주소 | `server/src/config/cors.ts:10`, `server/src/config/cors.test.ts:6`의 `brain-arena-xi.vercel.app` | 비밀값은 아니며 서비스 주소 노출 여부만 판단. CORS 설정을 무작정 지우면 접속에 영향 |
| 예방 | 환경 파일 제외 규칙 범위 | `.gitignore:3`의 `.env` | `.env.local`, `.env.production`도 제외하도록 `.env.*` 및 `!.env.example` 추가 권장. 현재 검사에서는 해당 실제 환경 파일 미발견 |

개인 작성자 정보가 포함된 커밋(최신순):

```text
2c1e3fe
2cffd59
60f2f31
c708164
6580c0d
957222e
26e080d
9d833d7
8ec1f6a
ffe37f1
e23131a
1c62cc0
```

## 검사 결과

- 현재 추적 파일 56개, 로컬 전체 ref에서 도달 가능한 커밋 12개, 과거 파일 경로 59개, 고유 파일 내용(blob) 149개를 검사했습니다. shallow clone은 아닙니다.
- 삭제되거나 변경된 과거 파일 내용도 포함하여 이메일, 한국 휴대전화·주민번호 형태, 개인 홈 경로, 대표적인 제공자 토큰, 개인키, 비밀번호·토큰 대입, 인증정보 포함 URL을 검색했습니다.
- 개인 작성자 이름·이메일의 파일 본문 포함 여부를 별도로 확인했으며 발견되지 않았습니다.
- 커밋 메시지에서 의심 키워드가 잡힌 두 건은 게임 이름 `Secret Dice`로 확인했습니다.
- 최초 커밋의 `client/tsconfig.node.tsbuildinfo:1`에서 주민번호 패턴처럼 보인 숫자 두 건은 64자리 16진수 해시 일부로 확인했습니다.
- `client/.env.example`, `server/.env.example`에는 localhost 주소와 포트만 있습니다. `render.yaml`에도 실제 인증정보는 발견되지 않았습니다.
- 이미지 두 개는 로고이며, PNG 청크에 EXIF·텍스트 메타데이터는 없었습니다.
- 추적되지 않은 일반 파일은 없었습니다. `.git`, `node_modules`, `dist`를 제외한 로컬 파일 목록에서 실제 `.env`, 키 파일, DB·CSV·로그·ZIP 후보는 발견되지 않았습니다.

## 공개 전 정리 순서

1. 공개용 이름과 본인 GitHub 계정의 noreply 이메일을 정합니다. 이후 커밋용 Git 설정을 먼저 바꿉니다.
2. 기존 이력을 보존하려면 별도 백업 후 12개 커밋의 Author와 Committer를 모두 교체합니다. 설정 변경이나 `.mailmap`만으로 기존 커밋 원본의 이메일이 제거되지는 않습니다.
3. 이력을 재작성하면 커밋 ID가 바뀌므로 원격 반영 및 협업자의 기존 복제본 처리까지 계획해야 합니다. 이력이 필요 없다면 검토한 현재 파일만 새 저장소의 첫 커밋으로 공개하는 방법도 있습니다.
4. 실제 원격의 모든 브랜치·태그, PR·이슈·릴리스·첨부파일·Actions 로그 등을 추가 확인하고 공개 여부를 변경합니다.

최초 점검에서는 보고서만 추가했습니다. 이후 수행한 보완과 로컬 이력 재작성, 원격 반영 준비 내역은 문서 상단에 기록했습니다. 저장소 공개 전환은 이번 작업에 포함하지 않습니다.

## 범위와 한계

로컬 `main`, `origin/main`, Codex ref에서 도달 가능한 이력을 대상으로 검사했습니다. 원격에서 새로 fetch하지 않았으므로 서버에만 있는 ref나 최신 변경은 확인하지 못했습니다. reflog 전용·도달 불가능 객체, 의존성·빌드 출력 전체, GitHub의 PR·이슈·릴리스·Actions·캐시는 검사하지 않았습니다. 전용 secret scanner는 설치되어 있지 않아 Python 정규식 검색과 수동 검토를 사용했습니다. 패턴이 없는 실명·주소, 임의 형식의 비밀값 등은 누락될 수 있습니다.
