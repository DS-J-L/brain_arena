{
  "name": "brain-arena",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "concurrently -n server,web -c cyan,magenta \"npm run dev -w brain-arena-server\" \"npm run dev -w brain-arena-web\"",
    "build": "npm run typecheck -w @brain-arena/shared && npm run build -w brain-arena-server && npm run build -w brain-arena-web",
    "test": "npm run test -w brain-arena-server"
  },
  "devDependencies": { "concurrently": "^9.1.2" }
}
